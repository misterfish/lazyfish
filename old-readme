``
log(
    lazyWake(
        lazyTake(10, lazyMap(
            square, lazyRange(5)))));
``

``
log(lazyWake(lazyTake(10, lazyMap(square, lazyRange(5)))));
``


lazy-range 5
    |> lazy-wake
    |> log

        range = lazy-range 5
        mapped = lazy-map square, range
        taken = lazy-take 10 mapped
        log taken

        log do
            lazy-take 10 do
                lazy-map square, do
                    lazy-range 5

        log lazy-take 10 lazy-map square, lazy-range 5

